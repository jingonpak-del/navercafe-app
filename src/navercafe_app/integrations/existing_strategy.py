"""Compatibility notes for the older local naver-cafe-strategy-tool project.

This module intentionally avoids importing the old local project by path. Copy stable code into
``navercafe_app`` modules or install the old project separately if direct reuse is required.
"""

LOCAL_STRATEGY_TOOL_PATH = "C:/Users/USER/naver-cafe-strategy-tool"
